#include<iostream>
using namespace std;

int sumIs(int arr[], int size){

    // base case
    if(size == 1 || size = 0){
        return arr[i];
    }
}

int main(){

    int arr[5] = {1,2,3,4,5};
    int size = 5;

    sumIs(arr, size);

    return 0;
}
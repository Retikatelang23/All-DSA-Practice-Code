#include <iostream>
using namespace std;

int main()
{

    // int num = 4;

    // cout << num << endl;

    // // address of operator -> to find the address of the value stored

    // cout << "the address of num is " << &num << endl;

    // int *ptr = &num;

    // cout << "the value is: " << *ptr << endl;
    // cout << "the address is: " << ptr << endl;

    // pointer to int is created pointing to some garbage value
    // int *p = 0;
    // cout << *p << endl;

   /* 
    int i = 9;
    int *r = &i;

    cout << r << endl;
    cout << *r << endl;


    //same like uper vala
    int *p = 0;
    p = &i;

    cout << p << endl;
    cout << *p << endl;
*/    

    // int num = 8;
    // int a = num;
    // cout<<"a. before "<<num<<endl;
    // a++;
    // cout<<"a. after "<<num<<endl;

    // int *p = &num;
    // cout<<"before "<<num<<endl;
    // (*p)++;
    // cout<<"after "<<num<<endl;

    // ek pointer ko dusre pointer mai copy karna hai
    //  int *q = p;
    //  cout << p << " - " << q << endl;
    //  cout << *p << " - " << *q << endl;


    // important concept
    // int i = 6;
    // int *t = &i;
    // cout << (*t)++ << endl;
    // cout << i++ << endl;

    // *t = *t + 1;
    // cout << *t << endl;

   
    // jitne bits mai stored hai utna aage badhega 
    // eg:- 100 location hai to vo 104 ho jayega --> integer --> 4bits 
    // cout << "before t " << t << endl;
    // t = t + 1;
    // cout << "after t " << t << endl;


    return 0;
}